/* jshint esversion: 8 */

const assert = require('chai').assert;

describe('index.js testing', () => {
  it('should have testing here but none was required', () => {
    assert.equal(true, true);
  });
});
